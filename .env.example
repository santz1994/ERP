# ============================================================================
# EXTERNAL PostgreSQL Configuration
# ============================================================================
# PostgreSQL is now EXTERNAL (not in Docker container)
# Install PostgreSQL 15 locally from: https://www.postgresql.org/download/windows/
#
# After installation:
# 1. Start PostgreSQL service
# 2. Create database: erp_quty_karunia
# 3. Update DATABASE_URL below with your credentials
#
# Connection string format:
# postgresql://username:password@host:port/database_name
#
# Examples:
# - Local: postgresql://postgres:password123@localhost:5432/erp_quty_karunia
# - Windows default: postgresql://postgres:@localhost:5432/erp_quty_karunia
# - Custom host: postgresql://user:pass@192.168.1.100:5432/erp_quty_karunia

# ⚠️ REQUIRED: Update this with your external PostgreSQL connection details
DATABASE_URL=postgresql://postgres:password@localhost:5432/erp_quty_karunia

# ============================================================================
# Redis Configuration (still in Docker)
# ============================================================================
REDIS_URL=redis://redis:6379/0

# ============================================================================
# Environment & Security
# ============================================================================
ENVIRONMENT=development
DEBUG=true
JWT_SECRET_KEY=your-secret-key-change-in-production

# ============================================================================
# Database Credentials (for reference - use DATABASE_URL above)
# ============================================================================
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=erp_quty_karunia

# ============================================================================
# Grafana Configuration
# ============================================================================
GRAFANA_PASSWORD=admin

# ============================================================================
# pgAdmin Configuration (removed from Docker)
# ============================================================================
# Use external DB client instead, or download pgAdmin standalone
# https://www.pgadmin.org/download/
PGADMIN_PASSWORD=password123
