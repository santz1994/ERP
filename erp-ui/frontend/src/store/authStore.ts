/**
 * Auth Store - Backward Compatibility Export
 * Re-exports useAuthStore from index.ts for backward compatibility
 * with existing imports like `import { useAuthStore } from '@/store/authStore'`
 */

export { useAuthStore } from './index'
